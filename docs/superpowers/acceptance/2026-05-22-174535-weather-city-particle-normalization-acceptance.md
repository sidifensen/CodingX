# 天气城市助词归一化验收标准

## 范围

- 本次仅验收天气 MCP 工具的城市解析与回归测试
- 不涉及前端页面与数据库结构变更

## 验收条目

1. 输入“北京的天气怎么样”时，天气工具不得再把 `city` 解析为“北京的”
2. 当地理编码服务仅接受 `name=北京` 时，工具仍可成功返回北京天气结果
3. 新增回归测试必须覆盖“X的天气”场景，且在修复前可复现失败、修复后通过
4. 后端验证命令必须通过：
   - `mvn compile`
   - `mvn test`
5. 所有新增或修改代码需补充必要注释，说明业务意图与边界约束
