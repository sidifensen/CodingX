# AGENTS Instructions

更新时间：2026-05-13

## Scope

本文件适用于 `D:\code\CodingX` 整个项目目录。

## Hard Rules

1. 阶段文档必须按阶段目录归档。  
每个阶段产物必须放在 `docs/project/plans/phase-*/` 对应目录中，不得放在其他目录。

2. `docs/project/plans` 根目录禁止新增阶段产物文件。  
根目录仅允许：
- 路线图总览文档
- 阶段文档规范文档

3. 每个阶段目录必须包含以下标准文件：
- `overview.md`
- `tasks.md`
- `acceptance.md`
- `progress-log.md`
- `decisions.md`

4. 新增阶段专题文档时，必须放在对应阶段目录内。  
命名建议使用：`topic-<name>.md`。

5. 更新阶段内容时，优先更新对应阶段目录文件，不要跨阶段混写在同一文件。

6. 前端技术栈固定为：
- `React`
- `Vite+`（https://viteplus.dev/）
- `Tailwind CSS`

7. 后端技术栈固定为：
- `Spring Boot`
- `Sa-Token`
- `Lombok`
- `Hutool`
- `OkHttp`

8. 分期顺序固定为：
- `Phase 1-3: Web`
- `Phase 4-5: Skill / MCP`
- `Phase 6-7: Electron`
- `Phase 8-9: 云端环境`

9. 每次功能完成后必须提交代码。  
每次完成一个可独立验收的功能、修复或文档规范调整后，都必须立即提交一次 Git 提交；提交信息必须使用中文，并且准确描述本次改动内容，禁止使用英文提交信息、无意义提交信息或将多个不相关改动混在同一次提交中。

10. 所有新增与修改代码都必须补充统一注释。  
必须遵循以下注释规范：
- 类、接口、枚举、记录类型必须有头部注释，说明职责与使用边界。
- 方法必须有注释，说明用途、关键参数、返回值与必要的异常/约束。
- 方法体内部必须按执行步骤补充注释，至少覆盖输入校验、核心处理、状态变更、持久化、外部调用、返回结果等关键阶段。
- 关键逻辑、分支判断、状态流转、数据库映射、第三方接口调用、并发/异步处理等位置必须加解释性注释。
- 参数、字段、记录类型入参、链式构造中的关键值，必要时可在同一行后补充行尾注释，但注释必须简洁且有实际信息量，禁止空洞注释。

11. 禁止直接使用原生 HTML 写法交付界面。  
前端界面、组件结构、模板示例与代码实现统一使用项目约定技术栈表达：
- Web 界面使用 `React` 组件与 `Tailwind CSS`
- 不要直接编写独立 HTML 页面、原生 HTML 模板字符串或以 HTML 作为主要交付形式
- 需要描述结构时，优先使用 `JSX`、组件代码或 `Markdown`

## Reference

详细规范见：

- `docs/project/plans/phase-docs-spec.md`
