## 多会话协作规范

- AI 每次执行任务时，如发现其他会话提交、新增或修改的内容，且这些内容不属于当前需求的必要改动范围，必须默认视为他人工作成果并保持原样。
- 对其他会话或与当前需求无关的既有改动，禁止修改、格式化、清理、重命名、移动、删除、回滚或纳入本次提交；仅在用户明确要求时才处理。
- 若其他会话的内容直接阻塞当前任务且无法绕开，必须先向用户说明冲突点，并等待明确指令后再处理。
- 修改文件时如发现本会话内容被其他会话改动、覆盖或出现冲突，必须立即停止并向用户说明，不得自行合并、回退或继续改写。
- 后端或前端测试失败时，如判断失败可能由其他会话的无关改动导致，应停止继续测试并说明原因，仅提交本会话实际修改的代码。

## Git提交信息规范

- 提交信息必须使用中文。
- 提交信息必须带前缀和模块名，格式为：`<前缀>(<模块名>): <中文说明>`
- 允许的前缀：`feat`、`fix`、`docs`、`refactor`、`test`、`chore`
- 模块名必须使用英文，使用当前模块名字
- 示例：`feat(chat): 完善意图解析与聊天工作区绑定能力`、`fix(auth): 修复登录会话过期问题`
- 完成任务后，默认由助手协助执行提交（`git add` + `git commit`）；
- 非简单任务（尤其是大功能开发、或前后端同时改动）在完成后必须自动提交，不需要用户额外提醒；
- 简单任务豁免范围内的小改动默认不提交代码；仅当用户明确要求提交时才执行 `git add` + `git commit`；

> **强制要求：除符合简单任务豁免规则外，开发前必须遵循以下 superpowers-plus 流程，禁止跳过。**

## superpowers-plus 简单任务豁免规则
- 可豁免：文档/注释/排版/格式化、元信息、局部小修正、UI交互、日志级别/日志打印调整、错误文案微调（不改变业务行为）
- 不可豁免：新功能、业务逻辑/接口/缓存变更、UI交互、多模块

## 简单任务执行规则

- 简单任务默认直接修改，不要求输出完整计划流程。
- 简单任务按改动面做最小必要验证，不做无关模块验证。
- 小改动（符合简单任务豁免范围）如需写计划时，仅需产出一个 `docs/superpowers/plans/` 下的 plan 文档；不强制要求 spec 与 acceptance 文档。

## 功能说明与文档维护规范

### 交付摘要

除简单任务外，每次完成开发后，助手最终回复必须包含完整的实现说明：

- **实现目标**：本次功能或修复解决了什么问题。
- **核心流程**：用 3-5 步说明主要链路，每一步必须写清“触发入口/输入、处理模块或方法、关键判断、状态或数据变化、输出结果”。禁止只写模块名加一句短描述，必须让维护者不看代码也能理解请求如何流转、失败如何兜底、状态如何更新。
- **关键文件**：列出主要修改文件及职责。
- **关键逻辑**：说明重要判断、边界条件、异常处理或数据流转。
- **验证结果**：说明执行了哪些测试、构建或浏览器验证。

核心流程的每一步建议使用 2-4 句描述，必要时补充关键字段、配置项、接口名或数据库状态变化。流程应覆盖正常路径、关键分支和主要异常路径；若某一步只是“调用某服务”“更新某状态”但没有说明调用条件、传入数据、返回结果或失败处理，视为信息不足。

交付摘要必须避免以下写法：

- “先校验参数，再调用服务，最后返回结果。”
- “A 组件负责选择，B 服务负责执行，失败后 fallback。”
- “更新数据库并通知前端。”

交付摘要推荐写法：

- “用户在聊天页发送消息后，前端将 `conversationId`、消息内容和当前模型参数提交到后端聊天接口；后端先校验会话归属与工作区状态，校验失败时通过全局异常处理器返回中文 `ApiResponse.message`，前端直接展示该消息。”
- “模型选择阶段先读取显式模型配置；未指定时按普通/深度思考模式分别回退到 `chat.defaultModel` 或 `chat.deepThinkingModel`，再按供应商可用性、thinking 能力和候选优先级过滤，最终生成有序候选列表。”

禁止只回复“已完成”“已修复”，必须让维护者能快速理解本次实现。

### 功能文档

新增功能、业务逻辑调整、接口/数据库/缓存变更、多模块联动时，必须新增或更新功能文档。

文档目录：

- `docs/features/index.md`：功能索引。
- `docs/features/<module>/<feature>.md`：具体功能说明。

功能文档只记录当前真实实现，内容保持简洁，建议包含：功能用途、使用入口、核心流程、关键文件、关键数据结构、测试/验证方式。

简单文案、样式、注释、日志、小范围修复，可不更新功能文档。

## superpowers-plus 标准流程

1. **brainstorming** — 需求澄清
2. **writing-plans** — 任务分解
3. **test-driven-development** — 先写失败测试，再写生产代码，遵循 RED-GREEN-REFACTOR
4. **executing-plans** — 按计划执行实现，过程严格遵循 TDD，并优先使用子 agent 分工推进
5. **requesting-code-review** — 关键里程碑完成后、提交前或合并前进行代码评审
6. **finishing-a-development-branch** — 任务完成后收尾

### 执行约束

- 默认情况下，**executing-plans** 阶段不开启分支树/工作树；如确需开启，必须先询问用户并获得明确同意。
- 如任务可安全拆分，应尽量使用子 agent 并行/分工执行，减少主会话上下文污染。
- 子 agent 仅用于边界清晰、职责独立、写入范围可控的子任务；若处于关键路径且结果需立即依赖，优先主会话直接处理。

### 核心原则

- **TDD 铁律**：没有失败测试就不写生产代码。
- **Debug 铁律**：没有根因分析就不修 bug。
- **流程优于猜测**：先验证再断言。
- **自我评估**：使用内联检查表自检，不发起子代理 review 循环。

### Hutool 工具规范

**强制要求：所有手写工具类代码必须优先使用 Hutool 替代，禁止重复造轮子。**

## 注释规范

- **强制要求：以后所有新增文件、以及所有被修改的文件，必须同步补充必要注释。**
- 注释应说明“业务意图、关键约束、边界条件、非直观实现原因”，禁止无意义注释（如“给变量赋值”）。
- 类、核心方法、复杂分支、异步/并发流程、跨模块调用点必须有可读注释，保证后续维护者可快速理解。
- 若本次改动未补齐必要注释，视为未完成，不得提交。

### 后端注释细则

- 后端所有新写或修改过的 Java 文件，必须补齐类注释、字段注释、构造器依赖注释、核心方法注释和必要的分支注释。
- `private final XxxService xxxService;` 这类依赖字段必须写明依赖用途，不能只写“依赖”两个字。
- `VO` / `DTO` / `PO` / `DO` / `Request` / `Response` 的字段必须补充注释，说明字段语义、来源、是否可空、默认值或状态含义；记录类型字段优先使用行尾注释或字段级注释，保证每个字段都能单独理解。
- 方法体内涉及状态流转、条件分支、组装入参、调用下游服务、异常兜底、资源释放的地方，必须按步骤写清处理过程；建议使用“步骤 1 / 步骤 2 / 步骤 3”的方式标明主链路。
- `Controller` 只负责协议适配、参数接收、权限/鉴权入口和响应封装，不允许编排复杂业务流程、聚合多服务规则、做大量数据转换或写入业务状态。
- `Controller` 中若出现超出协议层职责的逻辑，必须下沉到 `application service`、`domain service` 或 `support service`，再由 `Controller` 调用。
- 复杂查询、筛选、排序、校验、默认值回退、鉴权后的业务决策，优先抽到 `service` 层，不要在 `Controller` 中临时拼装。
- 新增或修改的后端代码如果已经存在局部注释风格，必须沿用同一文件中的风格并补齐缺失部分，避免同类文件出现混用和断层。

## 前端开发规范

- **强制要求：所有前端页面必须支持夜间模式。**
- 新增页面、改版页面与核心交互组件时，必须同时提供亮色/暗色两套主题表现，禁止仅实现单主题。
- 主题切换后必须保证可读性与可用性：文本、边框、背景、图标、悬浮态、禁用态在暗色下可辨识，不得出现透明叠底导致内容难以识别。
- 主题适配范围必须包含滚动条（页面滚动容器与组件内部滚动容器）：暗色模式下禁止出现系统默认亮色滚动条，滑块与轨道需可辨识且与主题令牌一致。
- 主题样式应优先复用统一设计令牌（颜色变量、间距、阴影、圆角等），避免在页面内散落硬编码颜色。

- **强制要求：前端页面设计必须避免卡片套卡片与过量卡片化布局。**
- 页面结构应优先根据业务密度选择分区、栅格、列表、表格、工具栏、抽屉、弹窗等合适形态；卡片仅用于承载真实独立对象、摘要信息或可重复条目，禁止把页面区块、表单容器、工具区域无差别包装成卡片。
- 禁止在卡片内部再嵌套视觉完整的卡片；如确需表达层级关系，应优先使用间距、分隔线、边框、背景层级、标题区、列表分组或表格结构，避免形成“卡片套卡片”的视觉噪音。
- 新增页面、改版页面与核心组件设计时，必须参考 `frontend-design` 与 `ui-ux-pro-max` 技能的设计原则，先明确业务场景、信息层级、用户路径、状态覆盖与设计令牌，再进入实现；禁止为了填充页面或制造复杂感生成大量无业务必要的卡片。

- **强制要求：前端页面开发必须优先组件化，并抽取可复用逻辑。**
- 页面文件只负责页面编排与路由级状态组织，通用展示块必须拆分为独立组件，避免单文件堆叠大量 JSX 与样式逻辑。
- 跨页面复用或同页多处复用的业务逻辑（请求编排、状态管理、副作用处理、数据转换）必须抽取到 `hooks`、`utils`、`services` 或领域模块，禁止重复粘贴实现。
- 组件拆分与逻辑抽取后，命名与职责边界必须清晰：一个组件/Hook 只解决一类问题，避免“万能组件”与“万能 Hook”。
- **强制要求：前端交互禁止使用浏览器原生弹窗。**
- 禁止使用 `window.alert`、`window.confirm`、`window.prompt` 作为正式交互方案。
- 重命名、删除确认、表单输入等场景必须使用项目内自定义弹窗、抽屉或浮层组件实现，保证样式、主题与交互一致。

## 错误处理与日志规范

- **强制要求：前端展示的错误信息必须优先使用后端返回的 `ApiResponse.message`，禁止前端自行改写后端错误语义。**
- 前端接口层必须统一封装响应解析与错误提取逻辑（集中在 `api` 层），页面/组件只消费统一异常结果，禁止各页面重复手写 `response.json()` + `throw`。
- **强制要求：后端所有接口异常必须通过全局异常处理器统一返回 `ApiResponse` 结构，错误文案统一中文。**
- **强制要求：后端异常日志必须统一格式打印，至少包含：错误码、HTTP 状态码、请求方法、请求路径、返回给前端的错误文案。**
- 对未知异常返回前端时必须使用通用中文提示，避免泄露堆栈与内部实现细节；详细堆栈仅保留在后端日志。

## 数据库变更规范

- **强制要求：凡涉及数据库结构变更（建表、删表、字段新增/删除/类型变更、索引/约束变更），必须同时提交迁移脚本与结构定义更新。**
- **强制要求：所有新建表必须补充表注释，所有新增字段必须补充字段注释；修改既有表结构时，若目标表或字段缺少注释，必须一并补齐。**
- **强制要求：数据库表注释与字段注释统一使用中文短语，注释内容末尾不加句号。**
- **强制要求：执行仓库中已编写的 `.sql` 文件时，必须优先通过 `mcp__postgres__` 完成，不得直接用 shell 直连数据库执行，以确保和项目数据库上下文、权限与事务控制保持一致。**
- 必须新增对应 `db/migration` 脚本（遵循现有命名规则），确保其他环境可自动补齐。
- 必须同步更新 `backend/src/main/resources/db/schema.sql`，保持基线结构与迁移一致。
- `backend/src/main/resources/db/schema.sql` 与迁移脚本中的表定义、字段定义必须同时包含注释语句，禁止提交无注释的表结构。
- 涉及表字段变更时，必须同步检查并更新对应的 PO/Mapper/DTO 映射，避免“数据库已变更但代码未生效”。
- 聊天会话的任务完成提醒已读状态必须写入数据库 `chat_conversation.task_completion_read`，默认值为 `1` 表示已读；后台任务完成后重置为 `0`，用户点击提醒或进入会话后更新为 `1`。
- 前端刷新后的提醒圆点必须优先使用后端返回的 `taskCompletionRead`，`seenTaskFinishedAtByConversationId` 仅允许作为本地运行会话或旧快照的兼容兜底。
- `chat_conversation.pinned` 只表示会话置顶排序，`chat_conversation.share_token` 只表示公开分享令牌，`chat_conversation.task_completion_read` 只表示任务完成提醒已读状态，三者都必须保留中文注释并保持语义独立，禁止在前端或后端代码里互相复用。

## 文档布局

- `docs/superpowers/specs/` — 设计规格文档（`YYYY-MM-DD-HHMMSS-<topic>-design.md`）
- `docs/superpowers/plans/` — 实现计划（`YYYY-MM-DD-HHMMSS-<feature>.md`）
- `docs/superpowers/acceptance/` — 验收标准文档（`YYYY-MM-DD-HHMMSS-<feature>-acceptance.md`）

```bash
# 后端服务 http://localhost:5001
cd backend && mvn spring-boot:run

# 用户前端 http://localhost:5002
cd frontend/user && npm run dev

# 管理端前端 http://localhost:5003
cd frontend/admin && npm run dev
```
启动前请先检查后端 `5001`、用户前端 `5002` 和管理端前端 `5003` 的占用情况；如对应端口已被占用，请先结束占用进程，再启动服务。

## 完成验证要求

提交/合并前按改动面验证：

1. **仅后端改动**：执行 `mvn compile`、`mvn test`
2. **仅前端改动**：默认执行 `npm run build`、`npm run test:run`
    - 若属于“简单任务豁免”且仅涉及样式/文案微调，可降级为：受影响测试文件定向执行 + CDP 视觉验证
3. **前后端都有改动**：四项全部执行
4. **浏览器验证（/web-access + CDP）**：仅在改动前端页面/交互，或用户明确要求时执行
5. **视觉改动强制项**（弹窗/下拉/日期时间选择器/主题切换）：必须补充 CDP 证据（截图或计算样式），未提供证据不得声明“已修复”

## 浏览器测试

若本次改动属于“简单任务豁免范围”（如文档/注释/排版/格式化、日志或文案微调且不改变业务行为），且未涉及前端页面/交互变更，则无需执行本节浏览器验证要求。

使用浏览器进行测试时，必须通过 `/web-access` skill，并使用 CDP 打开和操作浏览器；禁止绕过 CDP 直接声称已完成浏览器验证。

### 浏览器验证最小证据标准（强制）

1. 给出复现路径：页面 URL + 关键点击路径。
2. 给出结果证据：至少一张截图（保存到 `logs/`）或关键节点计算样式（如 `backgroundColor`、`opacity`）。
3. 对“透明/可读性”问题，必须明确验证：
    - 关键容器背景非透明，不会透出下层内容影响识别
    - 文本、边框与交互控件在当前主题下清晰可读
4. 验证失败时不得直接结束，必须继续定位并二次验证后再交付。
